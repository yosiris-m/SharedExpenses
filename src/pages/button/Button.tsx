import React from "react";

type Label = {
  label: string;
};
/*
function Button({ labe, type = "button", to = "", onClick = () => {} }) {
  const btn = (
    <button type="submit" className={styles.btn} onClick={onClick}>
      {label}
    </button>
  );

  if (to === "") {
    return btn;
  } else {
    return <Link to={to}>{btn}</Link>;
  }
}

export default Button;*/
